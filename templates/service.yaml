apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-poc-chart.fullname" . }}
  labels:
    {{- include "argocd-poc-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-poc-chart.selectorLabels" . | nindent 4 }}
